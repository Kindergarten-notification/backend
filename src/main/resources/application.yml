server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

  spring:
    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/kinder_notification?serverTimezone=Asia/Seoul
      username: root
      password: aotlf4%vmfh
#  mvc:
#    view:
#      prefix: /templates/
#      suffix: .mustache

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: none # create update none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    show-sql: true
    properties:
      hibernate:
        format_sql: true # formatting sql log


  profiles:
    include: mysql
